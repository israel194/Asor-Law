# תהליך בנייה (build) — חשוב לקרוא

## מצב נוכחי: ⚠️ ה-template אינו מסונכרן

`build.js` נועד לייצר את `he/index.html`, `en/index.html`, `ar/index.html`
מתוך `template.html` + `lang/{he,en,ar}.json` (החלפת `{{placeholders}}`).

**אבל כיום ארבעת המקורות נסחפו זה מזה** ואינם עקביים:

| מקור | מצב |
|------|-----|
| `he/en/ar/index.html` | **מקור-האמת** — נערכו ידנית (אבטחה, SEO, JSON-LD schema, תוכן) |
| `template.html` | ישן — חסר ~600 שורות לעומת התוכן החי |
| `lang/*.json` | חלקי — תוכן ה-HTML החי כבר לא תואם את הערכים בקבצים |

### למה אסור להריץ build.js כרגע
- בנייה מה-template הנוכחי **מוחקת ~600 שורות** תוכן חי מ-`he`.
- ערכי `lang/he.json` לא תואמים את ה-HTML החי, לכן בנייה **מדליפה עברית
  לעמוד האנגלי** (~160 שורות).

לכן `build.js` **חסום** ויסרב לרוץ ללא הדגל `--force`.

## איך לעבוד עד לסנכרון מלא
ערכו ישירות את `he/index.html` / `en/index.html` / `ar/index.html`.
הם הקבצים שנפרסים ל-GitHub Pages (asor-law.com).

## כדי לסנכרן מחדש (משימה ידנית, מומלץ עם סקירה)
1. בחרו את `he/index.html` כמבנה הקנוני.
2. עברו סעיף-סעיף: כל טקסט ניתן-לתרגום → `{{section.key}}`, ועדכנו את
   הערך המתאים בכל שלושת קבצי `lang/*.json` (he/en/ar) כך שיתאים לתוכן החי.
3. תוכן זהה בכל השפות (כותרות אבטחה, schema גנרי) → ריטרל ישיר ב-template.
4. אמתו: `node build.js --force` ואז `git diff` — `he` חייב לצאת ללא שינוי;
   `en/ar` צריכים להכיל תרגומים נכונים וללא עברית זרה.
5. רק לאחר שהאימות עובר — הסירו את חסימת ה-`--force` מ-`build.js`.
